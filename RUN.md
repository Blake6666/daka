# RUN.md —「全网热搜聚合」运行说明

> Day 7 初版 / **Day 8 更新**｜最后更新：2026-09-24
> 这份文件回答一个问题：**以后任何人（包括未来的你自己）拿到这份代码，怎么把它跑起来。**

## 一、环境要求（先确认再启动）

| 需要什么 | 怎么确认 | 没有的话 |
|---|---|---|
| Node.js（v18 以上） | 终端输入 `node --version`，能显示版本号 | 去 nodejs.org 下载安装 |
| npm（装 Node 时自带） | 终端输入 `npm --version` | 同上 |

## 二、启动步骤（共 3 步）

**第 1 步：进入前端文件夹**

```
cd frontend
```

（在项目根目录 `daka/` 下执行；如果在别的位置，先 `cd` 到 `C:\Users\Administrator\WorkBuddy\daka`）

**第 2 步：安装依赖（第一次运行才需要，之后可跳过）**

```
npm install
```

- 成功的样子：无红色报错，最后出现 `added 39 packages` 类似字样
- 依赖装在 `frontend/node_modules/`，它已被 `.gitignore` 挡住，**不会上传 GitHub**

**第 3 步：启动开发服务器**

```
npm run dev
```

- 成功的样子：两三秒内出现 `VITE v5.x ready`，下面有一行 `Local: http://localhost:5173/`

## 三、打开页面

| 设备 | 地址 | 说明 |
|---|---|---|
| 本机（电脑） | http://localhost:5173 | 日常开发用这个 |
| 同一 Wi-Fi 的手机/平板 | http://10.247.28.191:5173 | 手机看单栏版式用；`10.247.28.191` 是电脑在 Wi-Fi 里的地址，**重连 Wi-Fi 后可能变化**，变了就在电脑上重新查（或问 AI） |

- 端口固定 5173，写死在 `frontend/vite.config.js` 里（`strictPort: true`），不会漂移
- 停止服务器：在运行它的终端里按 `Ctrl + C`
- 页面打不开时先看这个：**Nano 等加速器的系统代理有时会把 localhost 也代理掉**，需要在该加速器设置里把 `localhost;127.0.0.1` 加进「不使用代理」列表

## 四、当前版本是什么状态（诚实标注）

| 项 | 状态 |
|---|---|
| 三栏榜单（抖音 / B站 / 百度，各 20 条，共 60 条） | ✅ 可用，**mock 假数据**（页面有「mock 数据」徽标，不冒充真数据） |
| 搜索框实时筛选（三个榜同时过滤，可与分类叠加） | ✅ 可用 |
| 刷新按钮（图标旋转 + 成功提示条） | ✅ 可用（mock 阶段 = 重新走一遍假加载，将来换成真请求代码不用改） |
| 深色 / 浅色模式切换 | ✅ 可用，选择存在浏览器 localStorage，下次打开保持 |
| 分类筛选标签（8 个，带条数徽标） | ✅ 可用 |
| 热搜词云（点词 = 只看该分类） | ✅ 可用。**词取自 mock 数据的「分类」字段，只有 7 个词**；接真接口后换成真实关键词，组件不用改 |
| 7 天热度趋势图 | ✅ 可用，手绘 SVG 零依赖。**纵轴是相对指数不是绝对热度**（三个平台量级差十几倍，直接比会把小平台压成一条直线） |
| Hero 统计区 | ⚠️ **部分**：「实时总热搜数」「热度最高词条」是实时算出来的真数；**「今日新增 18」是写死的假数字**，接真数据时再改成真算 |
| 点击标题新标签页跳原平台 | ✅ 可用（mock 数据统一指向各平台热榜页） |
| 收藏 + 备注 +「我的收藏」 | ✅ 可用，存在浏览器 localStorage，刷新不丢；**换浏览器/清缓存会没**（砍登录的预期代价） |
| 异常状态（4 种） | ✅ 可用，页面底部有「异常演示」开关可切换查看：正常 / 单榜失败 / 全部失败 / 空榜单 |
| 真实数据接入（云函数/数据库/定时抓取） | ❌ 未做——TECH_DESIGN.md 第 9 节 Q1 的既定降级：先跑通界面，数据结构已和云函数返回格式对齐，将来换真数据页面代码不用改 |
| 部署上线（公网地址） | ❌ 未做，课程后续安排 |

## 五、项目结构（速览）

```
daka/
├── frontend/               ← 前端工程（Day 7 新建）
│   ├── index.html           页面模板
│   ├── package.json         依赖清单 + 启动命令
│   ├── vite.config.js       开发服务器配置（端口 5173、允许局域网访问）
│   └── src/
│       ├── main.jsx         程序入口
│       ├── App.jsx          页面主体（导航栏/Hero/三栏榜单/分类/收藏/异常状态都在这）
│       ├── styles.css       全部样式（含深色模式）
│       ├── components/      ← Day 8 新建：可复用的小组件
│       │   ├── WordCloud.jsx     热搜词云
│       │   └── TrendChart.jsx    7 天热度趋势折线图（手绘 SVG）
│       ├── data/
│       │   └── hotlistData.js    示例榜单数据 + 7 天趋势数据（将来换真数据只动这里）
│       └── hooks/
│           ├── useHotlists.js     数据加载引擎（含异常模拟）
│           └── useFavorites.js   收藏存取引擎（localStorage）
├── research.md              Day 3 需求研究
├── PRD.md                   Day 4 产品需求
├── TECH_DESIGN.md           Day 5 技术设计
├── AGENTS.md                项目协作规则
└── RUN.md                   本文件
```

## 六、常见问题

- **端口被占用**：报 `Port 5173 is already in use` → 说明已经有一个在跑，直接开浏览器访问即可；或先关掉旧的
- **页面打不开**：确认终端里 `npm run dev` 还在运行（窗口没关、没报错）；再检查加速器代理有没有把 localhost 也代理了（见第三节最后一条）
- **手机访问不了**：确认手机和电脑连**同一个 Wi-Fi**；第一次访问可能被 Windows 防火墙拦（允许 Node.js 访问网络即可）；地址里的 IP 变了就重新查
- **搜了半天没结果**：搜索框的关键词要和热搜标题**逐字匹配**（比如搜「大学生」能中，搜「学生」也能中，但搜「高校」不中）。搜不到时每栏会显示「本榜没有…」的提示
- **切了深色想切回来**：点导航栏右上角的 ☀️ / 🌙 图标
- **收藏全没了**：清了浏览器缓存或换了浏览器——这是设计如此（见上表），不是 bug
